# AGENT.md — Руководство и контекст для AI-агентов

> **Для любого AI-агента:** Этот документ является первоисточником контекста проекта. Перед внесением любых изменений обязательно ознакомьтесь с архитектурой, картой файлов, правилами биржевой интеграции Bybit и логикой 3 вариантов торговой стратегии.

---

## 1. О проекте в двух словах

Репозиторий **`dca_bot`** представляет собой готовую алгоритмическую систему для торговли бессрочными фьючерсами Bybit (**Linear USDT Perpetuals**) на базе стратегии **«Манипуляция на часе» (1H Fibonacci Price Action & Dual Grid)**.

Система включает в себя:
1. **Боевой торговый бот Bybit V5** (`scripts/bybit_trader.py` + `indicators/pybit_client.py`) с поддержкой мульти-монетного трейдинга, авто-трейлинга сеток, защиты по SL/БУ и подхвата ордеров при перезапуске.
2. **Индикатор TradingView** (`indicators/fib_dual_grid.pine`) — визуальное отображение импульсов, сеток, свипов и сигналов.
3. **Интерактивный бэктестер и симулятор** (`scripts/backtest_strategy_interactive.py`, `scripts/backtest_manipulation_grid.py`).
4. **Модуль анализа волатильности и DCA** (`volatility_calc/`).
5. **Набор автоматических тестов** (`tests/test_bybit_trader.py`, 157 тестов).

---

## 2. Карта файлов и архитектура (File & Directory Map)

| Путь к файлу / каталогу | Назначение и ответственность |
| :--- | :--- |
| **`scripts/bybit_trader.py`** | **Главный исполнительный скрипт бота.** Загружает конфиг, сканирует рынок по заданным монетам, рассчитывает объемы под риск ($2.00), выставляет ордера (или симулирует в `--dry-run`), ведет непрерывный фоновый мониторинг (трейлинг, отмена висячих ордеров при SL, перенос в безубыток). |
| **`indicators/pybit_client.py`** | **Клиент Bybit API V5 Linear.** Автоматически определяет режим позиций (**Hedge Mode** `positionIdx: 1/2` vs **One-Way Mode** `0`), округляет цены и объемы по биржевым спецификациям (`tick_size`, `qty_step`), размещает (`place_order`), сдвигает (`amend_order`), отменяет (`cancel_order`, `cancel_all_orders`) и проверяет позиции (`get_position`). |
| **`config/trade_config.yaml`** | **Основной конфигурационный файл стратегии.** Содержит список торгуемых монет (`symbols`), суммарный риск (`total_risk_usd: 2.0`), буферы входа/тейка (`entry_buffer_pct: 0.07`, `tp_buffer_pct: 0.10`), пороги ложного пробоя и параметры шкалы Fib. |
| **`indicators/fib_dual_grid.pine`** | **Скрипт индикатора TradingView (Pine Script v5).** Полная визуальная реализация логики: поиск импульсов (вершина $H$ по абсолютному пику волны), отображение уровней Фибоначчи, отрисовка сеток, детектор ложного пробоя (Sweep Reclaim) с фильтром отскока MACD и тейком на 0.382 при наливе 0.618. |
| **`indicators/macd.py`** | Расчет гистограммы MACD (12, 26, 9) на pandas. Используется для подтверждения моментума (отскока) при ложном пробое. |
| **`scripts/backtest_strategy_interactive.py`** | Интерактивный бэктестер стратегии с Rich-таблицами, эмуляцией исполнения ордеров, расчетом логарифмических уровней Фибоначчи (`calc_fib`). |
| **`scripts/backtest_manipulation_grid.py`** | Бэктестер сетки манипуляции (1.618 / 2.000) на исторической выборке. |
| **`scripts/test_bybit_connection.py`** | Диагностический скрипт: проверка подключения к Bybit API V5 (Mainnet/Testnet), типа аккаунта (UNIFIED), доступного баланса и открытых позиций. |
| **`volatility_calc/`** | Модуль расчета исторической волатильности, симуляции покрытия DCA и оптимизации шага сетки. |
| **`tests/test_bybit_trader.py`** | Набор модульных тестов pytest (151 тест): форматирование символов, округления шага цены/лота, детекторы всех типов сетапов, расчет риска 50/50, тейк корзины 0.382. |
| **`strategiya-manipulyaciya-na-chase-gemini.md`** | Подробная спецификация 3 вариантов стратегии «Манипуляция на часе». |

---

## 3. Логика торговой стратегии: Три ключевых варианта

Стратегия работает на таймфрейме **1H** в направлении **LONG**.

### 3.0. Построение импульса
* **Старт ($L$):** `low` первой свечи импульса.
* **Вершина ($H$):** Абсолютный максимум волны по теням (`high`), вне зависимости от цвета свечи.
* **Минимальный импульс:** $\ge 2.0\%$ `(H - L) / L * 100%`.
* **Шкала Fib:** Логарифмическая: $\text{Price}(\text{level}) = \exp(\ln(H) - \text{level} \times (\ln(H) - \ln(L)))$.
* **Уровни:** $0.000 = H$, $1.000 = L$ (стоп сетки).

---

### ВАРИАНТ 1: Ложный пробой (Sweep Reclaim + MACD + Безубыток)
Сценарий краткосрочного снятия ликвидности за уровнем $1.000$:
1. **Условия:**
   * Тень свечи пробивает уровень $1.000$ не глубже чем на **0.5%** (`reclaim_max_sweep_pct: 0.5`).
   * **Ни одна свеча не закрывается телом ниже 1.000** (`reclaim_allow_close_below: false`).
   * **MACD Rebound:** Гистограмма MACD на свипе или реклейме показывает разворот вверх ($\text{Hist}[k] > \text{Hist}[k+1]$ или $\text{Hist}[0] > 0$).
2. **Вход:** На возврате цены выше уровня **1.000 Fib** (Reclaim).
3. **Стоп-Лосс:** Чуть ниже абсолютного минимума тени свипа (`SweepLow * 0.9995`).
4. **Тейк-Профит:** Уровень **0.618 Fib МИНУС 2.0%** (`Price(0.618) * 0.98`).
5. **Безубыток:** При росте цены до уровня **0.786 Fib** стоп-лосс автоматически переносится в безубыток: $\text{Вход} \times 1.0005$ (+0.05% на покрытие комиссий).

---

### ВАРИАНТ 2: Тройная сетка коррекции (Triple Grid) с Трейлингом и каскадным TP
Классическая коррекционная сетка внутри импульса (без пробоя 1.000):
1. **Ордер 1:** Уровень **0.500 Fib** $+0.07\%$ буфер входа. Тейк: **0.236 Fib** $-0.10\%$ буфер. При закрытии по TP ордера 2 и 3 отменяются.
2. **Ордер 2:** Уровень **0.618 Fib** $+0.07\%$ буфер входа. При наливе цель всей позиции снижается до **0.382 Fib** $-0.10\%$ буфер. Ордер 3 остается в стакане. При закрытии по TP Ордер 3 снимается.
3. **Ордер 3:** Уровень **0.786 Fib** $+0.07\%$ буфер входа. При глубоком сквизе до 0.786 цель для ВСЕХ трех ордеров снижается до **0.500 Fib** $-0.10\%$ буфер! Ордер 3 дает сверхприбыль, Ордер 2 закрывается в плюс, Ордер 1 выходит в безубыток.
4. **Стоп-Лосс:** Ровно на уровне **1.000 Fib**.
5. **Распределение риска:** Суммарный риск ($2.00) делится строго поровну:
   * Риск Ордера 1: $0.6667 ($q_1 \times (e_1 - \text{SL}) = 0.667$)
   * Риск Ордера 2: $0.6667 ($q_2 \times (e_2 - \text{SL}) = 0.667$)
   * Риск Ордера 3: $0.6667 ($q_3 \times (e_3 - \text{SL}) = 0.667$)
   * Итоговый суммарный риск при выбивании всей сетки по SL 1.000 strictly равен $2.00.
6. **Трейлинг:** Если цена обновляет вершину $H$ без отката к 0.500, бот пересчитывает уровни и сдвигает все три ордера выше через `amend_order`.
7. **Отмена ордеров:** При выбивании по SL или закрытии позиции оставшиеся незаполненные лимитные ордера немедленно снимаются.

---

### ВАРИАНТ 3: Сетка манипуляции (1.618 & 2.000)
Сценарий глубокого пробоя импульса (истинная манипуляция):
1. **Условия:** Выход цены за $1.000$ глубже **0.5%** ИЛИ закрытие часовой свечи телом ниже $1.000$.
2. **Ордер 1:** Уровень **1.618 Fib** $+0.07\%$ буфер.
3. **Ордер 2 (DCA):** Уровень **2.000 Fib** $+0.07\%$ буфер.
4. **Тейк-Профит:** Возврат к уровню **0.500 Fib** от исходного импульса.
5. **Стоп-Лосс:** Уровень **2.414 Fib** (в бэктестах также проверялся аварийный SL 5.0–6.0 или выдержка 30 дней).

---

## 4. Особенности интеграции Bybit V5 Linear (Критично для кода)

При модификации торгового кода строго соблюдайте следующие правила Bybit API:

### 4.1. Категория и формат символов
* Торговля ведется в категории **`category="linear"`** (USDT Perpetuals).
* Символы TradingView с суффиксами `.P` или `.PERP` (например, `NEARUSDT.P`, `ZECUSDT.P`) **обязательно форматируются** через функцию `format_symbol()` $\to$ `NEARUSDT`, `ZECUSDT`.

### 4.2. Режим позиций: Hedge Mode vs One-Way Mode
* На Bybit Unified аккаунтах часто включен **Hedge Mode (Dual-Side Position)**.
* В Hedge Mode:
  * Для Long (Buy): **обязательно** передавать **`positionIdx: 1`**.
  * Для Short (Sell): **обязательно** передавать **`positionIdx: 2`**.
* В One-Way Mode: передается **`positionIdx: 0`**.
* Метод `client.get_position_idx(symbol, side)` в `indicators/pybit_client.py` **автоматически кэширует и подставляет нужный `positionIdx`**, предотвращая ошибку Bybit `10001 (position idx not match position mode)`.

### 4.3. Авто-подхват существующих ордеров (Идемпотентность)
* При запуске или перезапуске бота в боевом режиме (`--live -y`) бот **сначала проверяет** `client.get_open_orders(sym)` и `client.get_position(sym)`.
* Если ордера (до 3) уже размещены на бирже, бот **не выставляет дубликаты**, а подключает найденные `orderId` к циклу мониторинга (при необходимости довыставляя недостающий Ордер 3).

### 4.5. Конечный автомат (State Machine) жизненного цикла сделки
Бот отслеживает полный жизненный цикл сделки через функцию `process_monitor_step()`:
1. **`TRAILING`**: ордера 0.500, 0.618 и 0.786 выставлены на биржу; при обновлении вершины импульса уровни сдвигаются выше.
2. **`O1_FILLED`**: налит Ордер 1 (0.500), TP позиции стоит на 0.236 Fib. Если цена разворачивается и берет TP 0.236 — бот немедленно снимает висящие Ордера 2 и 3 (`cancel_all_orders`) и переходит в `IDLE`.
3. **`O2_FILLED`**: при проливе налился Ордер 2 (0.618) — бот автоматически вызывает `client.set_position_tp_sl()` и переносит Take-Profit всей совокупной позиции на общий уровень **0.382 Fib**. Ордер 3 активен в стакане. При взятии TP 0.382 Ордер 3 снимается.
4. **`O3_FILLED`**: при глубоком сквизе налился Ордер 3 (0.786) — бот переносит Take-Profit позиции на общий уровень **0.500 Fib (-0.10%)**. Все 3 ордера закрываются в плюс/БУ.
5. **`AWAITING_SWEEP_CLOSE`**: при срабатывании стопа на 1.000 бот снимает все ордера и дожидается закрытия часовой свечи:
   - Если свеча закрылась **выше 1.000** (`close >= 1.000`) и закол $\le 0.5\%$ с разворотом MACD $\to$ вход в **Вариант 1 (Sweep Reclaim)**.
   - Если свеча закрылась **ниже 1.000** (`close < 1.000`) или закол $> 0.5\% \to$ выставление **Варианта 3 (Сетка манипуляции 1.618 / 2.000)** со стопом на 2.414 Fib.
6. **`IDLE`**: сделка завершена; на каждом закрытии новой свечи бот автоматически сканирует монеты на наличие новых импульсов $\ge 2.0\%$.

---

## 5. Как запускать и тестировать

### 5.1. Запуск тестов (Unit Tests)
```bash
uv run pytest -v
# Все 160 тестов должны проходить со 100% успехом
```

### 5.2. Режим симуляции (Dry-Run сканер)
Сканирует монеты из `config/trade_config.yaml` без выставления ордеров:
```bash
uv run python scripts/bybit_trader.py --dry-run
```

Переопределение монет через командную строку:
```bash
uv run python scripts/bybit_trader.py --symbols "NEARUSDT.P,SOLUSDT,BTCUSDT" --dry-run
```

### 5.3. Боевой режим (Live Trading)
Запуск торговли на реальном аккаунте Bybit в непрерывном фоновом цикле:
```bash
uv run python scripts/bybit_trader.py --live -y
```

Одиночный проход без непрерывного фонового цикла:
```bash
uv run python scripts/bybit_trader.py --live -y --once
```

### 5.4. Проверка подключения и баланса Bybit
```bash
uv run python scripts/test_bybit_connection.py
```
